# Turns the `engines.node` range in package.json into a hard requirement.
# Without this pnpm only prints a warning and installs anyway, which is easy to
# scroll past — and the failure then surfaces much later as a confusing build or
# runtime error instead of an upfront one.
engine-strict=true
