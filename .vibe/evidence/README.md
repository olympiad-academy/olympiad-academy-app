# .vibe/evidence

Verification evidence lives here under the DL-33 two-tier rule. Canonical Evidence Packet JSON, digests, and lineage are versioned. Bulky files belong under `sidecars/` (or the explicit binary patterns in `_gitignore`) and must be captured by `vibe-engineer archive` before cleanup.
