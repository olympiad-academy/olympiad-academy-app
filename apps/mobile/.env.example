# Expo reads EXPO_PUBLIC_* values from the mobile app directory at bundle time.
# Use a device-reachable absolute URL that includes the generated /api route prefix.
EXPO_PUBLIC_API_BASE_URL="http://127.0.0.1:3000/api"
